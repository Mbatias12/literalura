package com.alura.literalura.config;

import org.springframework.context.annotation.Configuration;

@Configuration
public class WebClientConfig {
    // Usamos WebClient.Builder provisto por Spring Boot (auto-config).
}